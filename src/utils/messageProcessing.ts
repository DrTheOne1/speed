import { supabase } from '../lib/supabase';

export async function sendMessage(message) {
  // Your existing sendMessage code...
}

export async function processScheduledMessages() {
  // Your existing processScheduledMessages code...
}